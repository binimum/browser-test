# browser-test

## Dev

1. Install packages `npm i`
2. Run dev server `npm run dev`
3. Build for Cloudflare Workers `npm run build`
4. Preview the build `npm run preview`
5. Static assets are stored at `public/` and are avaliable at `/` on the web

### Create a new test
